const express=require('express');const {body}=require('express-validator');const c=require('../controllers/booking.controller');const {authenticate}=require('../middleware/auth.middleware');const r=express.Router();r.use(authenticate);r.get('/',c.list);r.get('/history',c.history);r.post('/',[body('vehicleId').isInt({min:1}),body('slotType').optional().isIn(['CAR','BIKE','EV','HANDICAP'])],c.create);r.patch('/:id/cancel',c.cancel);module.exports=r;
