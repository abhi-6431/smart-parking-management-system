const express=require('express');const {body}=require('express-validator');const c=require('../controllers/payment.controller');const {authenticate}=require('../middleware/auth.middleware');const r=express.Router();r.use(authenticate);r.get('/',c.list);r.get('/:bookingId/receipt',c.receipt);r.patch('/:bookingId/pay',[body('paymentMethod').isIn(['CASH','UPI','CARD'])],c.pay);module.exports=r;
